# Build validation trigger

Temporary pull-request-only marker used to run the repository's independent TypeScript and Next.js build checks against the latest main source while Vercel is build-rate-limited.
